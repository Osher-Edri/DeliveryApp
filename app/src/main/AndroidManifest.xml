<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movingcompanymanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity-->
<!--            android:name=".Activities.nav_tasks"-->
<!--            android:label="@string/title_activity_nav_tasks"></activity>-->
        <activity android:name=".Activities.DriverMainActivity" />
        <activity android:name=".Activities.ManagerMainActivity" />
        <activity android:name=".Activities.NewTaskActivity" />
        <activity android:name=".Activities.TaskDetailsActivityManger" />
        <activity android:name=".Activities.DriverTasksListActivity" />
        <activity android:name=".Activities.SelectTaskDriverActivity" />
        <activity android:name=".Activities.AddWorkerActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>